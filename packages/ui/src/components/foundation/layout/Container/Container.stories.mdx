import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { DemoContent } from './__stories__/Example'
import Container from "./index";

<Meta title="Foundation/Layout/Container" component={Container} />

# Container

...

<ArgsTable story="Container Component" />

<Canvas>
  
    <Story
      name="Container Component"
      args={{
        ...Container.defaultProps,
        className: "bg-red-100"
      }}   
    >
      {({children, ...args}) => (
        <Container {...args}>
          {Boolean(children) ? children : <DemoContent />}
        </Container>
      )}
    </Story>
</Canvas>
