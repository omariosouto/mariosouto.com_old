import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import Box from './index';

<Meta
  title="Foundation/Layout/Box"
  component={Box}
/>

# Box

Box is a component that wraps common styles for create layouts.

<ArgsTable story="Box Component" />

<Canvas>
  <Story
    name="Box Component"
    args={{
      ...Box.defaultProps,
      children: 'Sample Content'
    }}
  >
    {(args) => (
      <Box {...args} />
    )} 
  </Story>
</Canvas>
