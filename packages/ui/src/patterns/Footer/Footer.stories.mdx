import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import Footer from './index';

<Meta
  title="Patterns/Footer (WIP)"
  component={Footer}
  argTypes={{}}
/>

# Footer

Footer component basic to be used cross all my personal projects.

<ArgsTable of={Footer} />

<Canvas>
  <Story
    name="Footer Component"
    args={{
      ...Footer.defaultProps,
    }}
  >
    {(args) => (
      <Footer {...args} />
    )} 
  </Story>
</Canvas>
